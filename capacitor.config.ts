import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.trimar.appfermentacao',
  appName: 'fermento certo',
  webDir: 'build'
};

export default config;
